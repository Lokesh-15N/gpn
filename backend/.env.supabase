# ============================================
# QUICK START CONFIGURATION FOR SUPABASE
# ============================================

# 1. Go to https://supabase.com and create a free account
# 2. Create a new project
# 3. Get your connection string from Project Settings → Database → Connection Pooling
# 4. Paste it below (replace the example)

# Database (Supabase Connection String)
DATABASE_URL=postgresql://postgres.[your-project-ref]:[YOUR-PASSWORD]@aws-0-us-east-1.pooler.supabase.com:5432/postgres

# Server Configuration
NODE_ENV=development
PORT=5000

# JWT Secret (IMPORTANT: Generate random 32+ character string)
# Generate one here: https://randomkeygen.com/
# Or run: node -e "console.log(require('crypto').randomBytes(32).toString('hex'))"
JWT_SECRET=REPLACE_WITH_YOUR_SUPER_SECRET_KEY_MIN_32_CHARS

# JWT Token Expiration
JWT_EXPIRES_IN=15m
JWT_REFRESH_EXPIRES_IN=7d

# CORS (Update to your frontend URL)
CORS_ORIGIN=http://localhost:5174

# Geo-fencing radius (meters)
DEFAULT_GEOFENCE_RADIUS=200

# Redis (Optional - Leave commented if not using Redis)
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_PASSWORD=

# Notification Services (Optional - for production)
# TWILIO_ACCOUNT_SID=
# TWILIO_AUTH_TOKEN=
# TWILIO_PHONE_NUMBER=
# WHATSAPP_API_KEY=
# FCM_SERVER_KEY=

# Rate Limiting
RATE_LIMIT_WINDOW_MS=60000
RATE_LIMIT_MAX_REQUESTS=100
